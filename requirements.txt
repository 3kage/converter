# GUI-залежності для запуску з вихідного коду.
# CLI (python -m converter) працює лише на стандартній бібліотеці Python.
#
# Встановлення:
#   pip install -r requirements.txt
#   або: pip install -e ".[gui]"
#
# Зовнішня утиліта (обовʼязково): FFmpeg (ffmpeg + ffprobe)
#   Windows: winget install Gyan.FFmpeg
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg

customtkinter>=5.2.0
darkdetect>=0.8.0
Pillow>=10.0.0
tkinterdnd2>=0.4.2
windnd>=1.0.7; sys_platform == "win32"
